image:
  tag: "1.0"

ingress:
  hosts:
    - host: staging-dummytest.martinn.no
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: staging-dummytest-tls
      hosts:
        - staging-dummytest.martinn.no