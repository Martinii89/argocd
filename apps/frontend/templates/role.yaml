apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "dummy-app.fullname" . }}-job-reader
  labels:
    {{- include "dummy-app.labels" . | nindent 4 }}
rules:
- apiGroups: ["batch"]
  resources: ["jobs"]
  verbs: ["get", "list", "watch"]
