apiVersion: v1
kind: Service
metadata:
  name: {{ include "rllobby.fullname" . }}
  labels:
    {{- include "rllobby.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rllobby.selectorLabels" . | nindent 4 }}
